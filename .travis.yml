language: elixir
elixir:
  - '1.7.4'
otp_release:
  - '21.1.4'

services:
  - postgresql

install:
  - mix local.rebar --force
  - mix local.hex --force
  - make fetch_dependencies

before_script:
  - make migrate_ci

script:
  - make test_play
